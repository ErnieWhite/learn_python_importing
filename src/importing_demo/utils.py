"""Helper utilities used within the package and useful to re-export."""

def greet(name: str) -> str:
    """Return a greeting string (used by examples)."""
    return f"Hello, {name}!"

# Show best-practice: keep module code simple and import-safe